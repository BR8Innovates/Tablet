{
  "name": "vitals-health-tracker",
  "version": "1.0.0",
  "description": "Vitals - Health & Activity Tracker (PWA + Android APK wrapper)",
  "private": true,
  "scripts": {
    "android:sync": "cap sync android",
    "android:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
